apiVersion: v1
kind: Service
metadata:
  name: {{ include "tschart.fullname" . }}
  labels:
    {{- include "tschart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tschart.selectorLabels" . | nindent 4 }}
