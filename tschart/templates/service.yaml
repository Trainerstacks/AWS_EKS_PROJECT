apiVersion: v1
kind: Service
metadata:
  name: {{ include "ts-chart.fullname" . }}
  labels:
    {{- include "ts-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ts-chart.selectorLabels" . | nindent 4 }}
